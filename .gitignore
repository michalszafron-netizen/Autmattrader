# Secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node (if we add JS tooling)
node_modules/
package-lock.json

# Local MCP config — contains API keys, do not commit
.mcp.json

# Bybit MCP — downloaded npm package, rebuild with: cd bybit-mcp && npm ci
bybit-mcp/

# Claude Code session data — local only
.claude/

# Temp output files
out.txt
myfxbook_test_out.txt

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Scraped content (Firecrawl output)
.firecrawl/

# Cached API responses (econ calendar fallback)
.cache/

# Logs & local data
logs/*.log
data/*.db
data/*.sqlite
data/*.parquet
*.pid

# Auto-generated reports and context
reports/
context/

# Temp scripts (debug/test)
tmp_*.py
tmp_*.txt
tmp_*.ps1

# Runtime junk / local-only artifacts (nie naleza do repo)
.codegraph/
dashboard/history.db
dashboard/*.db
out2.txt
out3.txt
positions.json
scripts/btc_1h_data.json
*.zip

# OS / build
dist/
build/
*.egg

# Bazy danych runtime (54MB, WAL/shm to zywe pliki) — nie wersjonowac
data/
*.db
*.db-shm
*.db-wal
